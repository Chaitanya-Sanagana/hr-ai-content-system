# Default demo mode does not require secrets.
# Add real keys only in your local .env file or hosting provider secrets.
# Do not commit real API keys.

LLM_ENABLED=false
LLM_PROVIDER=
LLM_API_KEY=
LLM_MODEL_NAME=
